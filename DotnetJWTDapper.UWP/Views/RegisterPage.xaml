<Page
    x:Class="DotnetJWTDapper.UWP.Views.RegisterPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DotnetJWTDapper.UWP.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Ellipse Width="150" Height="150">
                <Ellipse.Fill>
                    <ImageBrush ImageSource="/Assets/Square150x150Logo.scale-200.png" />
                </Ellipse.Fill>
            </Ellipse>
        </StackPanel>
        <StackPanel Grid.Row="2" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBox PlaceholderText="Name" Text="{x:Bind User.Name, Mode=TwoWay}" ></TextBox>
            <TextBox PlaceholderText="Username" Text="{x:Bind User.Username, Mode=TwoWay}" ></TextBox>
            <TextBox PlaceholderText="Email" Text="{x:Bind User.Email, Mode=TwoWay}" ></TextBox>
            <PasswordBox PlaceholderText="Password" Password="{x:Bind User.Password, Mode=TwoWay}" ></PasswordBox>
        </StackPanel>
        <StackPanel Grid.Row="3" HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="10">
            <Button Content="Register" HorizontalAlignment="Center" Click="Register_Click"></Button>
            <Button Content="Back" HorizontalAlignment="Center" Click="Back_Click"></Button>
        </StackPanel>
    </Grid>
</Page>
